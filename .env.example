# PostgreSQL
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DB=equivalencias

# Backend
NODE_ENV=production
PORT=3000
PORT_FALLBACK=3001,3002,3003
FRONTEND_PORT=3100
FRONTEND_BIND_IP=127.0.0.1
ALLOWED_ORIGINS=http://localhost:3100

# Seguridad (JWT)
JWT_SECRET=escribe-una-clave-secreta-larga-y-segura
JWT_EXPIRES_IN=8h
INVENTORY_SYNC_TOKEN=escribe-un-token-para-sync-de-inventario
CLOUDFLARED_TUNNEL_TOKEN=pega-aqui-el-token-del-tunnel-remoto

# Usuario Administrador por Defecto
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=escribe-una-password-segura
DEFAULT_ADMIN_ROLE=admin
DEFAULT_ADMIN_NAME=Administrador

# Solo para scripts externos de sincronización (el backend no consulta SQL Server directo)
INVENTORY_DB_USER=tu_usuario
INVENTORY_DB_PASSWORD=tu_password
INVENTORY_DB_SERVER=tu_servidor
INVENTORY_DB_NAME=tu_base_de_datos
INVENTORY_DB_PORT=1433
